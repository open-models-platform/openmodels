import Layout from '~/layouts/DefaultGuideLayout'

export const meta = {
  id: 'cli',
  title: 'Local Dev with CLI',
  description: 'Developing locally using the OpenModels CLI.',
  subtitle: 'Developing locally using the OpenModels CLI.',
  sidebar_label: 'Overview',
}

You can use the OpenModels CLI to run the entire OpenModels stack locally on your machine, simply by running `supabase init` and then `supabase start`.

The OpenModels CLI provides tools to develop your project locally, deploy to the OpenModels Platform, handle database migrations, and generate types directly from your database schema.

## Resources

<div className="grid md:grid-cols-12 gap-4 not-prose">
  {integrations.map((x) => (
    <div className="col-span-6" key={x.href}>
      <Link href={x.href} passHref>
        <GlassPanel icon={'/docs/img/icons/github-icon'} hasLightIcon={true} title={x.name}>
          {x.description}
        </GlassPanel>
      </Link>
    </div>
  ))}
</div>

export const integrations = [
  {
    name: 'OpenModels CLI',
    description:
      'The OpenModels CLI provides tools to develop manage your OpenModels projects from your local machine.',
    href: 'https://github.com/supabase/cli',
  },
  {
    name: 'GitHub Action',
    description: ' A GitHub action for interacting with your OpenModels projects using the CLI.',
    href: 'https://github.com/supabase/setup-cli',
  },
]

export const Page = ({ children }) => <Layout meta={meta} children={children} hideToc={true} />

export default Page
